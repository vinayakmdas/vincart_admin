import 'package:flutter/material.dart';

class Usermanagement extends StatelessWidget {
  const Usermanagement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("user manangement"),
      ),
    );
  }
}